import { SFCWithInstall } from '@utils/type'
import <%= options.fileName %> from './src/<%= options.fileName %>.vue'

<%= options.fileName %>.install = (app) => {
  app.component(<%= options.fileName %>.name, <%= options.fileName %>);
}

const _<%= options.fileName %>: SFCWithInstall<typeof <%= options.fileName %>> = <%= options.fileName %>

export default _<%= options.fileName %>