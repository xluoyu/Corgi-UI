<template>
  <div 
    class="cg-<%= options.name%>"
  >

  </div>
</template>

<script lang="ts">
import { defineComponent, computed, inject } from 'vue'
import styleVar from './styleVar'
import { getComponentCssVar, getGlobalCssVar } from '@corgi/utils/index'
import { IThemeCssVar } from '@corgi/utils/type'

export default defineComponent({
  name: 'Cg<%= options.fileName %>',
  props: {
    type: {
      type: String,
      default: 'default'
    }
  },
  setup () {
    const customTheme = inject<IThemeCssVar>('theme', null)
    const globalCssVar = getGlobalCssVar(customTheme)
    let cssVar = computed(() => {
      const componentCssVar = getComponentCssVar(customTheme, styleVar, '<%= options.fileName %>')
      if (props.color) {
        composeVar.color = props.color
      }
      return componentCssVar
    })

    return {
      cssVar
    }
  }
})
</script>

<style lang="less" scoped>

</style>